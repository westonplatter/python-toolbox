<%= simple_form_for(@category) do |f| %>

  <%= f.select :parent_id,
      nested_set_options(Category, @category) {|i| "#{'-' * i.level} #{i.name}" }.unshift(["No Parent", nil]) %>


  <br>

  <%= f.input :name %>

  <br>

  <%= f.submit "Create/Update", class: "btn btn-primary" %>

<% end %>
